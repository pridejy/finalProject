/*
document.addEventListener("DOMContentLoaded", function(){
    var checkbox = document.getElementById("idSave");
    var idInput = document.getElementById("id"); // 해당 id가 실제로 정의되어 있는지 확인이 필요합니다.

    // 아이디 저장 체크했을 때 이미지 바꾸기
    checkbox.addEventListener("click", function(){
        var label = this.nextElementSibling;
        var customCheckbox = label.querySelector(".form-check-input");

        if (this.checked) {
            customCheckbox.style.background = "url(resources/images/png/checkmark.png) no-repeat";
        } else {
            customCheckbox.style.background = "url(resources/images/png/check-mark.png) no-repeat";
        }
    });
});
*/